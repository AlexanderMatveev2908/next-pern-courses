{
  "name": "next-pern-courses",
  "packageManager": "yarn@4.9.2",
  "scripts": {
    "install-vercel": "corepack enable && corepack prepare yarn@4.9.1 --activate && yarn install",
    "dev": "yarn dlx prisma generate --schema=./apps/server/prisma/schema.prisma && yarn workspace shared build && turbo dev",
    "build": "yarn dlx prisma generate --schema=./apps/server/prisma/schema.prisma && yarn workspace shared build && turbo build --parallel",
    "build-client": "turbo build --filter=client... --force",
    "start": "turbo start",
    "license-fix": "find . -name package.json -not -path '*/node_modules/*' -exec sh -c 'jq \".license = \\\"UNLICENSED\\\"\" \"$1\" > \"$1.tmp\" && mv \"$1.tmp\" \"$1\"' _ {} \\;",
    "check": "yarn turbo check"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10",
    "turbo": "^2.5.4"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "immutable": "^5.1.3"
  }
}
