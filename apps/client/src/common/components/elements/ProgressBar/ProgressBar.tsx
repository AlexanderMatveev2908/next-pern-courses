/** @jsxImportSource @emotion/react */
"use client";

import type { FC } from "react";

type PropsType = {
  totSwaps: number;
  currSwap: number;
};

const ProgressBar: FC<PropsType> = ({ currSwap, totSwaps }) => {
  return <div></div>;
};

export default ProgressBar;
