/** @jsxImportSource @emotion/react */
"use client";

import type { FC } from "react";

type PropsType = {
  nHits?: number;
  totPages?: number;
};

const RowBtnCounter: FC<PropsType> = ({ nHits, totPages }) => {
  return <div></div>;
};

export default RowBtnCounter;
